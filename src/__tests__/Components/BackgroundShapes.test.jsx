import React from 'react'
import { render } from '@testing-library/react'
import BackgroundShapes from '../../Components/BackgroundShapes'

describe('BackgroundShapes test', () => {
    it('render', () => {
        render(<BackgroundShapes />)

        //screen.debug()
    })
})
